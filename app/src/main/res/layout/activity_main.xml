<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="0dp"
    android:paddingLeft="0dp"
    android:paddingRight="0dp"
    android:paddingTop="0dp"
    tools:context="com.example.kenert.allinoneapp.MainActivity">

    <ImageButton
        app:srcCompat="@android:drawable/btn_dialog"
        android:id="@+id/flashlightButton"
        android:adjustViewBounds="false"
        android:layout_height="350dp"
        android:layout_width="450dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:onClick="flashlightButtonClicked" />

    <ImageButton
        android:layout_height="50dp"
        app:srcCompat="@android:drawable/btn_dialog"
        android:id="@+id/notepadButton"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="342dp"
        android:layout_width="160dp"
        android:onClick="notepadButtonClicked" />

    <ImageButton
        app:srcCompat="@android:drawable/btn_dialog"
        android:id="@+id/calculatorButton"
        android:onClick="calculatorButtonClicked"
        android:layout_height="150dp"
        android:layout_width="200dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_alignBottom="@+id/notepadButton" />
</RelativeLayout>
